# מדריך פריסה מלא — מהמחשב המקומי לאינטרנט

> **הפרויקט:** אתר מתכונים · React + Vite (לקוח) · Node.js + Express (שרת) · MongoDB Atlas (מסד)
> **היעד:** קוד ב-GitHub, שרת על Render, לקוח על Vercel, מסד על Atlas — הכל ב-tier חינמי.
> **זמן משוער:** 45–60 דקות בפעם הראשונה.

---

## תוכן עניינים

- [שלב 0: לפני שמתחילים — 3 בדיקות אבטחה](#שלב-0)
- [שלב 1: Git ו-GitHub](#שלב-1-git-ו-github)
- [שלב 2: הכנת הקוד לפרודקשן](#שלב-2-הכנת-הקוד-לפרודקשן)
- [שלב 3: MongoDB Atlas לפרודקשן](#שלב-3-mongodb-atlas-לפרודקשן)
- [שלב 4: פריסת השרת ל-Render](#שלב-4-פריסת-השרת-ל-render)
- [שלב 5: פריסת הלקוח ל-Vercel](#שלב-5-פריסת-הלקוח-ל-vercel)
- [שלב 6: חיבור סופי (הצ'יק-אנד-אג של CORS)](#שלב-6-חיבור-סופי)
- [שלב 7: בדיקת קבלה](#שלב-7-בדיקת-קבלה)
- [מגבלות ידועות ב-tier החינמי](#מגבלות-ידועות)
- [פתרון תקלות נפוצות](#פתרון-תקלות-נפוצות)

---

## שלב 0

### שלוש בדיקות אבטחה לפני הפקודה הראשונה

**‏1. ודא ש-`.env` לא ייכנס ל-git.**
בפרויקט שלך זה כבר מסודר — יש שלושה קובצי `.gitignore`:

| קובץ | מה הוא חוסם |
|------|--------------|
| `.gitignore` (ראשי, בשורש) | `.env` בכל תיקייה, `node_modules/`, `dist/`, `uploads/`, `.claude/` |
| `backend/.gitignore` | `node_modules`, `.env`, `uploads` |
| `frontend/.gitignore` | `node_modules`, `dist`, `.env`, לוגים |

הקובץ הראשי נוצר עכשיו במיוחד לקראת הפריסה. הכלל החשוב בו:

```gitignore
.env
.env.*
!.env.example        # ← זה כן נכנס: תבנית בלי ערכים אמיתיים
```

**‏2. אימות בפועל — אל תסתמך על ההנחה.**
מיד אחרי `git init` (שלב 1), הרץ את הפקודה הזו **לפני** הקומיט הראשון:

```bash
git status --porcelain | grep -i "\.env$"
```

- **אין פלט** = מצוין, ה-`.env` מוסתר. המשך.
- **יש פלט** = עצור. ה-`.gitignore` לא תופס. אל תעשה commit עד שתתקן.

**‏3. הסיסמה שלך ב-Atlas.**
מכיוון שהפרויקט **מעולם לא היה ב-git** עד עכשיו (אימתתי — לא הייתה תיקיית `.git`), הסיסמה שלך לא דלפה להיסטוריה. אתה נקי.

> ⚠️ **אם בעתיד תעשה commit ל-`.env` בטעות:** לא מספיק למחוק אותו בקומיט הבא — הסיסמה נשארת בהיסטוריית git לנצח וניתנת לשליפה. במקרה כזה החלף מיד את הסיסמה ב-Atlas ואת ה-`JWT_SECRET`. זו הסיבה שהבדיקה בסעיף 2 חשובה.

---

## שלב 1: Git ו-GitHub

### 1.1 — מה נכנס ומה לא

| ✅ נכנס ל-git | ❌ לא נכנס |
|---------------|-----------|
| כל קוד המקור (`src/`, `routes/`, `models/`…) | `.env` — **סודות** |
| `package.json` + `package-lock.json` | `node_modules/` — ~200MB, נבנה מחדש |
| `.env.example` — תבנית בלי ערכים | `dist/` — נבנה מחדש בפריסה |
| `vercel.json`, קובצי תצורה | `uploads/` — תמונות שנוצרות בזמן ריצה |
| קובצי תיעוד (`.md`) | `.claude/`, `.vscode/`, `.DS_Store` |

**למה `package-lock.json` כן נכנס?** הוא נועל את הגרסאות המדויקות של כל תלות. בלעדיו, Render עלול להתקין גרסה חדשה יותר של ספרייה ולשבור לך את ה-build בלי שנגעת בקוד.

### 1.2 — אתחול ופוש (הפקודות המדויקות)

פתח טרמינל בתיקיית **שורש הפרויקט** (`פרויקט עם כלוד`, זו שמכילה את `backend` ו-`frontend`):

```bash
# 1. אתחול מאגר מקומי
git init

# 2. ← בדיקת האבטחה. חייב להחזיר כלום.
git status --porcelain | grep -i "\.env$"

# 3. הצגת מה עומד להיכנס (עבור ברפרוף, ודא שאין .env ואין node_modules)
git status

# 4. הוספת כל הקבצים למעקב
git add .

# 5. בדיקה אחרונה: רשימת הקבצים שבאמת ייכנסו לקומיט
git status --short

# 6. קומיט ראשון
git commit -m "Initial commit: Recipe app (React + Express + MongoDB)"

# 7. שינוי שם הענף ל-main (הסטנדרט ב-GitHub)
git branch -M main
```

> אם git מבקש ממך להגדיר זהות, הרץ פעם אחת:
> ```bash
> git config --global user.name "השם שלך"
> git config --global user.email "semoel1141@gmail.com"
> ```

### 1.3 — יצירת המאגר ב-GitHub

1. היכנס ל-**github.com** → לחץ על ה-**`+`** בפינה הימנית העליונה → **New repository**.
2. מלא:
   - **Repository name:** `recipe-app` (או כל שם באנגלית, בלי רווחים)
   - **Description:** אופציונלי
   - **Public** — נדרש כדי שאפשר יהיה להציג את הפרויקט; ה-`.env` לא שם, אז זה בטוח.
   - ⚠️ **אל תסמן** `Add a README`, `Add .gitignore`, או `Choose a license`. סימון שלהם ייצור קומיט בצד השרת ויגרום להתנגשות בפוש הראשון.
3. **Create repository**.
4. בעמוד שנפתח, העתק את הכתובת מתחת ל-**"…or push an existing repository from the command line"**.

### 1.4 — חיבור ופוש

חזור לטרמינל (החלף `<username>` ו-`<repo>` בשלך):

```bash
git remote add origin https://github.com/<username>/<repo>.git
git push -u origin main
```

בפעם הראשונה ייפתח חלון להתחברות ל-GitHub — התחבר בדפדפן.

> **אם תתבקש סיסמה בטרמינל:** GitHub לא מקבל סיסמת חשבון מאז 2021. אפשרויות: (א) התקן [GitHub CLI](https://cli.github.com/) והרץ `gh auth login` (הכי פשוט), או (ב) צור Personal Access Token ב-Settings → Developer settings → Tokens והשתמש בו כסיסמה.

**מכאן ואילך,** כל שינוי עתידי:
```bash
git add .
git commit -m "תיאור השינוי"
git push
```

---

## שלב 2: הכנת הקוד לפרודקשן

חדשות טובות: **הקוד שלך כבר בנוי נכון לפרודקשן ברוב המובנים.** אין בו אף כתובת `localhost` קשיחה — הכל עובר דרך משתני סביבה. להלן מה שכן צריך תשומת לב.

### 2.1 — קריאות API: כבר מוכן ✅

`frontend/src/api/axios.js` קורא את הכתובת ממשתנה סביבה:

```js
const baseURL = import.meta.env.VITE_API_URL;
```

**לא צריך לשנות שום שורת קוד.** רק להגדיר את המשתנה ב-Vercel (שלב 5).

> ⚠️ **נקודה קריטית להבנה:** Vite מזריק את `VITE_API_URL` לתוך קובצי ה-JS **בזמן ה-build**, לא בזמן ריצה. המשמעות: אם תשנה את המשתנה ב-Vercel אחרי שהאתר כבר נבנה — **חייב לעשות Redeploy**, אחרת הכתובת הישנה נשארת "צרובה" בקוד.

### 2.2 — CORS בשרת: כבר מוכן, צריך רק להגדיר ✅

`backend/app.js` כבר קורא רשימת דומיינים מותרים ממשתנה סביבה:

```js
const allowedOrigins = (process.env.CLIENT_URL || 'http://localhost:5173,http://localhost:5174')
  .split(',').map(o => o.trim()).filter(Boolean);
```

⚠️ **ה-`.env` המקומי שלך לא מגדיר `CLIENT_URL` כרגע** — הוא נופל לברירת המחדל (localhost), וזה בסדר גמור לפיתוח. **בפרודקשן חובה להגדיר אותו** לכתובת ה-Vercel שלך, אחרת הדפדפן יחסום כל בקשה. זה נעשה בשלב 6.

### 2.3 — הפורט: כבר מוכן ✅

`backend/server.js`:
```js
const PORT = process.env.PORT || 5000;
```
Render מזריק `PORT` אוטומטית. אל תגדיר `PORT` ידנית ב-Render — תן לו לנהל את זה.

### 2.4 — `trust proxy`: כבר מוכן ✅

`app.js` מגדיר `app.set('trust proxy', 1)` — נדרש מאחורי ה-proxy של Render כדי ש-rate limiting יזהה IP אמיתי ולא את ה-proxy.

### 2.5 — תיקון שביצעתי עכשיו: DNS 🔧

`backend/config/db.js` הכיל כפיית DNS של גוגל:
```js
dns.setServers(['8.8.8.8']);   // היה: רץ תמיד
```
זה היה workaround ל-Windows בלבד. **בקונטיינר לינוקס של Render זה מזיק** — הוא עוקף את ה-DNS הפנימי ומוסיף נקודת כשל. שיניתי לתנאי:

```js
if (process.platform === 'win32') {
  dns.setServers(['8.8.8.8']);
}
```
המחשב שלך ממשיך לעבוד בדיוק כמו קודם; Render מקבל התנהגות נכונה. (28/28 הבדיקות עוברות אחרי השינוי.)

### 2.6 — תיקון שביצעתי עכשיו: ניתוב SPA 🔧

React Router עם `BrowserRouter` מציג `/recipes/123` ככתובת אמיתית. אבל בשרת סטטי, רענון הדף על הכתובת הזו מחפש קובץ בשם `recipes/123` — שלא קיים — ומחזיר **404**.

יצרתי `frontend/vercel.json`:
```json
{ "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }] }
```
זה אומר ל-Vercel: "כל כתובת — הגש את `index.html`, ותן ל-React Router להחליט". **בלי הקובץ הזה, כל קישור ישיר למתכון היה שבור.**

> **אם תבחר Netlify במקום Vercel:** צור `frontend/public/_redirects` עם השורה: `/*  /index.html  200`

### 2.7 — מה עדיין דורש החלטה שלך ⚠️

**תמונות ה-AI נמחקות בכל דיפלוי.** ב-Render free tier מערכת הקבצים היא *ephemeral* — כל הפעלה מחדש מוחקת את `backend/uploads/`. משמעות: תמונה שנוצרה ב-AI ונשמרה במתכון תיעלם תוך שעות.

**החדשות הטובות:** הקוד שלך כבר מטפל בזה בחן — `RecipeImage.jsx` נופל לפלייסהולדר אם התמונה נשברת, ו-`/generate-image` נופל אוטומטית לתצלום אמיתי מ-TheMealDB (כתובת חיצונית שלא נמחקת).

**ההמלצה שלי לפרויקט גמר:** אל תשקיע בזה עכשיו. פשוט **אל תגדיר `GEMINI_API_KEY` ב-Render** — כך כל התמונות יגיעו מ-TheMealDB (כתובות חיצוניות יציבות), ולא תהיה בעיה כלל. אם תרצה יצירת תמונות AI אמיתית בהמשך, הפתרון הנכון הוא Cloudinary (יש tier חינמי) במקום כתיבה לדיסק.

---

## שלב 3: MongoDB Atlas לפרודקשן

אתה כבר משתמש ב-Atlas מקומית, אז המסד קיים. צריך רק שינוי אחד:

1. היכנס ל-**cloud.mongodb.com**.
2. בתפריט הימני: **Network Access** → **Add IP Address**.
3. לחץ **ALLOW ACCESS FROM ANYWHERE** (מזין `0.0.0.0/0`) → **Confirm**.

> **למה זה נדרש ולמה זה מקובל:** לשרתי Render בתוכנית החינמית אין כתובת IP קבועה — היא משתנה בכל הפעלה, כך שאי אפשר להגדיר רשימה לבנה ממוקדת. ההגנה על המסד נשארת שם המשתמש והסיסמה החזקים ב-`MONGO_URI`. זו הדרך הסטנדרטית לחבר Render ל-Atlas.

**ודא שמחרוזת החיבור שלך כוללת שם מסד.** בדוק שה-`MONGO_URI` נראה כך:
```
mongodb+srv://user:pass@cluster.mongodb.net/recipeapp?retryWrites=true&w=majority
                                                    ↑ שם המסד — חייב להיות
```
אם אין שם מסד אחרי ה-`/`, Mongo ישתמש ב-`test` כברירת מחדל.

---

## שלב 4: פריסת השרת ל-Render

### 4.1 — יצירת השירות

1. היכנס ל-**render.com** → **Sign up with GitHub** (מחבר את המאגרים אוטומטית).
2. בלוח הבקרה: **New +** → **Web Service**.
3. בחר את המאגר `recipe-app` → **Connect**.

### 4.2 — ההגדרות (שים לב במיוחד ל-Root Directory)

| שדה | ערך | הערה |
|------|------|------|
| **Name** | `recipe-app-api` | יהפוך לכתובת: `recipe-app-api.onrender.com` |
| **Region** | `Frankfurt (EU Central)` | הכי קרוב לישראל — זמן תגובה נמוך יותר |
| **Branch** | `main` | |
| **Root Directory** | **`backend`** | ⚠️ **הכי חשוב.** בלי זה Render יחפש `package.json` בשורש ולא ימצא |
| **Runtime** | `Node` | מזוהה אוטומטית |
| **Build Command** | `npm install` | |
| **Start Command** | `npm start` | מריץ `node server.js` — כבר מוגדר ב-`package.json` |
| **Instance Type** | `Free` | |

### 4.3 — משתני הסביבה

גלול ל-**Environment Variables** ולחץ **Add Environment Variable** לכל אחד:

| Key | Value | הערות |
|-----|-------|--------|
| `MONGO_URI` | מחרוזת החיבור מ-Atlas | **העתק מה-`.env` המקומי שלך** |
| `JWT_SECRET` | מחרוזת אקראית באורך 64 תווים | ראה למטה |
| `NODE_ENV` | `production` | |
| `CLIENT_URL` | **השאר ריק בינתיים** | נמלא בשלב 6 — עוד אין כתובת Vercel |

**אל תוסיף `PORT`** — Render מזריק אותו לבד.

**אל תוסיף `GEMINI_API_KEY`** — ראה ההמלצה בסעיף 2.7 (התמונות יגיעו מ-TheMealDB במקום).

**ליצירת `JWT_SECRET` חזק,** הרץ במחשב שלך:
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

> 💡 **המלצה:** השתמש ב-`JWT_SECRET` **שונה** מזה שבמחשב המקומי. כך גם אם הסוד המקומי נחשף אי-פעם, הפרודקשן מוגן. (תופעת לוואי: משתמשים שהתחברו מקומית יצטרכו להתחבר מחדש בפרודקשן — זה תקין ורצוי.)

### 4.4 — פריסה

לחץ **Create Web Service**. הפריסה לוקחת 2–5 דקות. עקוב בלשונית **Logs** — אתה מחפש:

```
MongoDB Connected: cluster0-shard-00-01.xxxxx.mongodb.net
Server running on port 10000
```

אם אתה רואה את זה — השרת חי. שמור את הכתובת שלו:
`https://recipe-app-api.onrender.com`

**בדיקה מהירה:** פתח את הכתובת בדפדפן. אתה אמור לראות: `Recipe App API is running...`

---

## שלב 5: פריסת הלקוח ל-Vercel

1. היכנס ל-**vercel.com** → **Continue with GitHub**.
2. **Add New…** → **Project** → בחר את `recipe-app` → **Import**.
3. הגדר:

| שדה | ערך |
|------|------|
| **Framework Preset** | `Vite` (מזוהה אוטומטית) |
| **Root Directory** | לחץ **Edit** ובחר **`frontend`** ⚠️ קריטי |
| **Build Command** | `npm run build` (ברירת מחדל) |
| **Output Directory** | `dist` (ברירת מחדל) |

4. פתח את **Environment Variables** והוסף:

| Key | Value |
|-----|-------|
| `VITE_API_URL` | `https://recipe-app-api.onrender.com/api` |

> ⚠️ **שתי טעויות נפוצות כאן:**
> - **חובה `/api` בסוף** — כל הנתיבים בקוד יחסיים אליו (`/recipes`, `/auth/login`).
> - **בלי `/` אחרי `api`** — כתוב `.../api` ולא `.../api/`.

5. לחץ **Deploy**. אחרי דקה-שתיים תקבל כתובת כמו:
   `https://recipe-app-xyz.vercel.app`

**העתק את הכתובת הזו — צריך אותה מיד בשלב הבא.**

---

## שלב 6: חיבור סופי

זהו הצעד שכולם שוכחים. כרגע הלקוח יודע לפנות לשרת, **אבל השרת עדיין לא מרשה ללקוח לפנות אליו** (CORS). האתר ייראה תקין אבל לא ייטען שום מתכון.

1. חזור ל-**Render** → השירות `recipe-app-api` → **Environment**.
2. ערוך את `CLIENT_URL` (או הוסף אותו) עם כתובת ה-Vercel שלך — **בלי `/` בסוף**:

```
CLIENT_URL=https://recipe-app-xyz.vercel.app
```

3. **Save Changes**. Render יפרוס מחדש אוטומטית (~2 דקות).

> 💡 **טיפ:** Vercel יוצר כתובת חדשה לכל preview deployment. אם תרצה שגם הן יעבדו, אפשר להזין כמה כתובות מופרדות בפסיק — הקוד שלך כבר תומך בזה:
> ```
> CLIENT_URL=https://recipe-app-xyz.vercel.app,https://recipe-app-git-main-user.vercel.app
> ```

---

## שלב 7: בדיקת קבלה

עבור על הרשימה באתר החי (`https://recipe-app-xyz.vercel.app`):

- [ ] **דף הבית נטען** ומציג מתכונים (טעינה ראשונה עשויה לקחת ~50 שניות — ראה "מגבלות ידועות")
- [ ] **חיפוש** מסנן תוצאות תוך כדי הקלדה
- [ ] **הרשמה** של משתמש חדש עובדת
- [ ] **התחברות/התנתקות** עובדות
- [ ] **יצירת מתכון** חדש נשמרת ומופיעה ברשימה
- [ ] **עריכה ומחיקה** של מתכון עובדות
- [ ] **"המתכונים שלי"** מציג רק את שלך
- [ ] 🔑 **רענון (F5) בתוך עמוד מתכון** — לא מקבל 404 (זה מאמת ש-`vercel.json` עובד)
- [ ] **קונסול הדפדפן** (F12) נקי משגיאות CORS
- [ ] **כתובת שגויה** (`/blabla`) מציגה את דף 404 היפה

---

## מגבלות ידועות

אלה מגבלות של ה-tier החינמי, לא באגים. **כדאי להזכיר אותן בהגנה על הפרויקט** — זה מראה הבנה של הסביבה.

| מגבלה | ההשפעה | פתרון |
|--------|---------|--------|
| **Cold start ב-Render** | השרת "נרדם" אחרי 15 דק' חוסר פעילות. הבקשה הראשונה אחרי זה לוקחת **~50 שניות** | פתח את האתר 2 דקות לפני הצגה. או: שדרוג ל-$7/חודש |
| **דיסק זמני** | `uploads/` נמחק בכל דיפלוי | כבר עקפנו — התמונות מגיעות מ-TheMealDB (סעיף 2.7) |
| **Atlas M0** | 512MB אחסון | מספיק בהחלט לאלפי מתכונים |

---

## פתרון תקלות נפוצות

### 🔴 "Failed to fetch" / שגיאת CORS בקונסול
**סיבה:** `CLIENT_URL` ב-Render לא תואם **בדיוק** לכתובת ה-Vercel.
**בדוק:** האם יש `/` מיותר בסוף? האם זה `https` ולא `http`? האם עשית Save ב-Render והפריסה הסתיימה?

### 🔴 האתר נטען אבל אין מתכונים, והקונסול מראה 404 על `/recipes`
**סיבה:** `VITE_API_URL` חסר את הסיומת `/api`.
**תיקון:** תקן ב-Vercel → **Deployments** → הדיפלוי האחרון → **⋯** → **Redeploy** (חובה! המשתנה נצרב בזמן build).

### 🔴 רענון בעמוד מתכון מחזיר 404
**סיבה:** `vercel.json` לא נמצא או ש-Root Directory לא הוגדר ל-`frontend`.
**בדוק:** שהקובץ קיים ב-git (`git ls-files frontend/vercel.json`).

### 🔴 Render: "Cannot find module 'express'"
**סיבה:** **Root Directory** לא הוגדר ל-`backend`.
**תיקון:** Settings → Root Directory → `backend` → Save.

### 🔴 Render: "MongoServerError: bad auth"
**סיבה:** סיסמה שגויה ב-`MONGO_URI`, או תווים מיוחדים בסיסמה.
**תיקון:** אם הסיסמה מכילה `@`, `#`, `/` או `:` — צריך לקודד אותם ב-URL-encoding (למשל `@` → `%40`). הכי פשוט: שנה ב-Atlas לסיסמה עם אותיות ומספרים בלבד.

### 🔴 השרת קורס מיד עם "חסרים משתני סביבה חיוניים"
**סיבה:** זו הודעה מ-`config/env.js` שלך — היא עובדת כמתוכנן ומצילה אותך מבאג סמוי.
**תיקון:** ודא ש-`MONGO_URI` ו-`JWT_SECRET` מוגדרים ב-Render → Environment.

### 🔴 "יותר מדי ניסיונות התחברות"
לא באג — זה ה-rate limiter שלך (30 ניסיונות / 15 דק'). המתן 15 דקות.

---

## סיכום זרימת העבודה מהיום

```
עורך קוד במחשב
        ↓
git add . && git commit -m "..." && git push
        ↓
GitHub (main)
        ↓ (webhook אוטומטי)
   ┌────┴────┐
   ↓         ↓
Render     Vercel
(backend)  (frontend)
```

**פריסה אוטומטית:** מרגע שהכל מחובר, כל `git push` ל-`main` מפעיל פריסה מחדש בשני השירותים אוטומטית. אין צורך לעשות שום דבר ידנית.
