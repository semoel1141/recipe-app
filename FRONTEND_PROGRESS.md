# Frontend Progress - Recipe App

מעקב אחרי מה שנעשה בצד הלקוח (React).

## סטטוס נוכחי
🟢 שלד ראשוני מלא: הרשמה/התחברות, רשימת מתכונים, צפייה/יצירה/עריכה. נבדק בדפדפן ועובד.

## מה עשינו עד כה

### הקמת פרויקט + תשתית ✅
- נוצר פרויקט `frontend/` עם Vite + React (React 19, Vite 8).
- הותקנו `react-router-dom` ו-`axios`.
- **הערה על אבטחה:** `npm audit` הראה 2 פגיעויות "high" ב-react-router. בדקנו: הן קשורות ל-RSC mode / SSR (React Server Components, Server Actions) - פיצ'רים שאנחנו לא משתמשים בהם באפליקציית SPA רגילה מבוססת Vite. השארנו את הגרסה העדכנית (7.18.1) במקום להוריד גרסה (ניסינו להוריד ל-7.11.0 כדי "לתקן", אבל זה חשף המון פגיעויות ישנות נוספות - עדיף העדכני).
- `src/api/axios.js` - instance של axios עם `baseURL` מ-`VITE_API_URL` (ב-`.env`), ו-interceptor שמצמיד `Authorization: Bearer <token>` מ-localStorage לכל בקשה.
- `src/context/AuthContext.jsx` - Context עם `user`, `login`, `register`, `logout`. שומר `token` ו-`user` ב-localStorage כדי לשרוד רענון דף.
- `index.html` - נוסף `lang="he" dir="rtl"` (חסר במקור מה-template של Vite, גרם לפריסה לא נכונה של טקסט עברי).

### עמודים ורכיבים ✅
- `components/Navbar.jsx` - קישורי ניווט, משתנה לפי מצב התחברות (הרשמה/התחברות מול "מתכון חדש"/שם משתמש/התנתקות).
- `components/PrivateRoute.jsx` - מגן על routes שדורשים התחברות, מפנה ל-`/login` אם אין משתמש.
- `pages/Login.jsx`, `pages/Register.jsx` - טפסים מול `/api/auth/login` ו-`/api/auth/register`.
- `pages/RecipeList.jsx` (עמוד הבית) - `GET /api/recipes`, רשימת קישורים למתכונים.
- `pages/RecipeDetail.jsx` - `GET /api/recipes/:id`, מציג כפתורי "עריכה"/"מחיקה" רק אם המשתמש המחובר הוא הבעלים (`user._id === recipe.owner`) או אדמין.
- `pages/RecipeForm.jsx` - טופס משותף ליצירה ולעריכה (לפי קיום `:id` ב-route). שדה מרכיבים הוא textarea (שורה = מרכיב אחד).
- `App.jsx` - כל ה-routes, `main.jsx` עוטף ב-`BrowserRouter` + `AuthProvider`.

### בדיקה בדפדפן ✅
נבדק ידנית עם Claude in Chrome מול הבקאנד האמיתי (חייב שהבקאנד ירוץ על פורט 5000):
- הרשמה → מחובר אוטומטית, שם המשתמש מופיע ב-navbar.
- יצירת מתכון → מפנה לעמוד הפרטים, כל השדות מוצגים נכון (כולל עברית).
- עריכה → הטופס נטען עם הערכים הקיימים, שמירה מעדכנת ומפנה בחזרה לעמוד הפרטים.
- התנתקות → כפתורי עריכה/מחיקה נעלמים; ניסיון גישה ל-`/recipes/new` בלי התחברות מפנה ל-`/login` (PrivateRoute).
- **הערה טכנית:** בכלי האוטומציה של הדפדפן, קליק לפי קואורדינטות על כפתורים לפעמים לא הפעיל את ה-handler (לא קשור לקוד שלנו) - קליק דרך `element.click()` ב-JS עבד באופן עקבי. אם יהיה צורך לבדוק שוב בעתיד, זו הדרך האמינה יותר.
- נוקו משתמש ומתכון הבדיקה מה-DB בסוף.

### חיפוש/סינון מתכונים ✅
- `pages/RecipeList.jsx` - נוסף שדה חיפוש (`input type="search"`) מעל הרשימה. מחפש לפי שם מתכון או מרכיב, מול `GET /api/recipes?search=...` בבקאנד.
- debounce של 300ms - לא שולחים בקשה על כל הקשת מקש, רק אחרי שהמשתמש עוצר להקליד רגע.
- הודעת "לא נמצאו מתכונים תואמים" כשהחיפוש לא מחזיר כלום (שונה מ"עדיין אין מתכונים" כשהרשימה ריקה לגמרי).
- נבדק בדפדפן מול נתונים אמיתיים: חיפוש לפי מרכיב הראה תוצאה נכונה, וחיפוש בלי התאמה הראה את הודעת "לא נמצא".

### הצגת יוצר המתכון + מצבי "שולח..." בטפסים ✅
- הבקאנד עכשיו מחזיר `owner` בתור `{ _id, name }` (populate) - עודכן `RecipeList.jsx` ו-`RecipeDetail.jsx` בהתאם (כולל תיקון בדיקת הבעלות `user._id === recipe.owner._id`).
- `RecipeList.jsx` - כל מתכון ברשימה מציג `נוצר ע"י <שם>`.
- `RecipeDetail.jsx` - שורת המטא-דאטה כוללת גם את שם היוצר.
- `Login.jsx`, `Register.jsx`, `RecipeForm.jsx` - נוסף מצב `submitting`: הכפתור מושבת ומראה טקסט זמני ("מתחבר...", "נרשם...", "שומר...") כדי למנוע לחיצה כפולה בזמן שהבקשה בתהליך.

### עיצוב מלוטש יותר ✅
- רשימת המתכונים עברה מרשימת קישורים פשוטה לגריד של כרטיסים (`recipe-grid` / `recipe-card`) עם hover effect (הצללה + הרמה קלה).
- טפסים (התחברות/הרשמה/מתכון) ועמוד פרטי מתכון עטופים בכרטיס עם רקע, מסגרת ו-shadow (משתמש במשתני ה-CSS `--code-bg`/`--shadow` שכבר היו מוגדרים מה-template של Vite, כדי לשמור על תמיכה אוטומטית ב-light/dark mode).
- כפתורים וקישורים קיבלו מצבי hover/focus עקביים.
- נבדק ויזואלית בדפדפן (dark mode) - נראה תקין.

### עיצוב "מטורף" - צבעוני ושמח, סגנון מגזין אוכל ✅
לבקשת המשתמש, עיצוב מחדש נועז יותר מהגרסה הקודמת (שהייתה מלוטשת אבל שמרנית):
- גופנים: `Rubik` (טקסט, תומך עברית מלא) ו-`Fredoka` (כותרות, לטינית בלבד - בעברית נופל חזרה ל-Rubik) מ-Google Fonts, נטענים ב-`index.html`.
- פלטת צבעים חמה: כתום (`--accent`) / ורוד-אדום (`--accent-2`) / צהוב (`--accent-3`), עם גרדיאנט (`--gradient`) שחוזר בכל האתר. הפלטה מוגדרת פעמיים ב-`index.css` (light + dark מ-`prefers-color-scheme`) כך שהעיצוב עובד בשני המצבים.
- Navbar עם רקע גרדיאנט sticky, הלוגו מקבל אנימציית "wiggle" קטנה ב-hover.
- כרטיסי מתכונים (`recipe-card`): פס צבעוני עליון שמתחלף בין 4 צבעים לפי `nth-child`, אימוג'י גדול שמתחלף במחזוריות (עגבניה/לחם/פיצה וכו'), hover עם הרמה + סיבוב קל.
- כפתורים: pill-shaped עם רקע גרדיאנט, hover מגדיל+מרים עם צל, active לוחץ פנימה.
- שדה החיפוש ותוויות המרכיבים בעמוד מתכון בודד: צורת pill.
- טפסים ועמוד מתכון בודד: כרטיס מעוגל עם פס גרדיאנט דק בראש הכרטיס.
- נבדק ויזואלית בדפדפן (dark mode) על כל העמודים המרכזיים - עקבי ונראה טוב.
- **הערה:** גופני Google Fonts נטענים מרשת חיצונית (fonts.googleapis.com) - באתר production אמיתי כדאי לשקול אם זה מקובל מבחינת פרטיות/ביצועים, או self-host לגופנים.

### תמונות למתכונים ✅
- `pages/RecipeForm.jsx` - נוסף שדה אופציונלי "קישור לתמונה" (`type="url"`) ליצירה ולעריכה.
- `pages/RecipeList.jsx` - כרטיס מתכון מציג את `recipe.imageUrl` בתור `<img>` בראש הכרטיס (140px גובה, `object-fit: cover`); אם אין תמונה או שהיא נכשלת לטעון (`onError`), נופל בחזרה לאימוג'י הצבעוני שכבר היה.
- `pages/RecipeDetail.jsx` - אותה לוגיקה, תמונה גדולה יותר (240px) שפורצת את ריפוד הכרטיס לכיסוי רוחב מלא ("full bleed"), עם fallback דומה אם הקישור שבור.
- מעקב אחרי תמונות שנכשלו נשמר ב-state (`brokenImages` / `imageBroken`) כדי לא לנסות לטעון שוב תמונה שכבר נכשלה.

### מעבר מלא ל-Tailwind CSS + עיצוב מקצועי מחדש ✅
לבקשת המשתמש: מעבר מעיצוב ה-CSS הרגיל (הצבעוני/"מטורף" הקודם) ל-Tailwind CSS, עם כיוון עיצובי הפוך - מינימליסטי, "אנושי", כזה שנראה כמו עיצוב UI/UX מקצועי ולא תבנית גנרית.
- הותקן `tailwindcss` + `@tailwindcss/vite` (גרסה 4 - בלי `tailwind.config.js`/`postcss.config.js`, ההגדרה היא ב-`vite.config.js` (plugin) ו-`@import "tailwindcss";` ב-`index.css`).
- `App.css` הוסר לגמרי - כל העיצוב עכשיו ב-classes של Tailwind בתוך ה-JSX.
- פלטה: רקע `stone-50`/`white`, טקסט `slate-800`/`slate-500` (לא שחור מוחלט), צבע מבטא יחיד `amber-700` לכפתורים/קישורים.
- `pages/RecipeList.jsx` - גריד בנטו לא-סימטרי: `grid-cols-1 md:grid-cols-12` עם דפוס `col-span` חוזר (7/5/4/4/4) כדי שכרטיסים יהיו בגדלים שונים. כרטיס "featured" (הרחב) מוצג אופקית (תמונה בצד, טקסט לידה) ומתחלף כיוון (`sm:flex-row` / `sm:flex-row-reverse`) בכל סבב; שאר הכרטיסים אנכיים (תמונה למעלה, טקסט למטה) - כדי "לשבור סימטריה" ולערבב בין תצוגות לפי הבקשה.
- `components/RecipeImage.jsx` - קומפוננטה משותפת חדשה (בשימוש גם ב-List וגם ב-Detail) שמנהלת נפילה חינה לפלייסהולדר עדין (איקון קווי כלים בגוון `stone`) אם אין `imageUrl` או שהטעינה נכשלה - מונע כפילות קוד.
- אנימציות/אינטראקציה "רכה": `transition-all duration-300 hover:-translate-y-1 hover:shadow-lg hover:scale-[1.01]` על הכרטיסים, ו-`hover:scale-105 duration-500` על התמונה בתוכם (zoom עדין).
- פינות: `rounded-3xl` לכרטיסים/עמודים גדולים, `rounded-xl`/`rounded-full` לשדות וכפתורים. צללים: `shadow-sm` בלבד (לא `shadow-lg` כברירת מחדל, רק ב-hover).
- טיפוגרפיה: כותרות `font-bold tracking-tight`, טקסט משני `font-light text-slate-500 leading-relaxed` - היררכיה ברורה בלי עומס משקלים.
- גופן: הוסר `Fredoka` (לא תמך בעברית ולא שימש בפועל) - נשאר רק `Rubik` במגוון משקלים (300-800) לכל הטקסט, גם כותרות וגם גוף.
- **לגבי תמונות מ-Unsplash:** המשתמש ביקש קישורי `source.unsplash.com` - בדקתי ומצאתי שהשירות **הושבת** (מחזיר 503, "Service Unavailable" - זה שירות ישן של Unsplash שהופסק ב-2023). לא השתמשתי בו כי הוא לא עובד. השארתי את התמונות הקיימות מ-LoremFlickr (ראו סעיף "שדה imageUrl" ב-`BACKEND_PROGRESS.md`) שכבר עובדות ונבדקו.
- נבדק ויזואלית בדפדפן: עמוד הבית (גריד + כרטיס featured), עמוד מתכון בודד (hero image + רשימת מרכיבים כ-bullets עדינים), טופס יצירה - כולם עקביים ונראים מקצועיים.

### עיצוב בהשראת Lurpak ✅
המשתמש הפנה לאתר המתכונים של Lurpak כרפרנס. גלשתי לאתר, בחנתי אותו (עמוד רשימה + עמוד מתכון בודד) ויישמתי את המאפיינים המרכזיים:
- **גריד תמונות צפוף**: `grid-cols-2 md:grid-cols-4` עם `gap-1` (כמעט בלי רווח) במקום הכרטיסים המרווחים הקודמים. כל כרטיס ביחס `aspect-[3/4]`, התמונה ממלאת אותו, ומעליה שכבת גרדיאנט (`bg-gradient-to-t from-black/80`) עם שם המתכון וזמן ההכנה בלבן במרכז התחתית.
- **עמוד מתכון בודד**: hero תמונה במלוא רוחב המסך (45-55vh) עם שם המתכון בלבן על גרדיאנט, ומתחתיו פריסת שתי עמודות - הוראות ההכנה ברוחב מלא משמאל, וסרגל מרכיבים על רקע `stone-100` בצד.
- **פלטה מונוכרומטית**: הוסר הכתום (`amber`) - הכל עכשיו `stone`/לבן/שחור, עם כפתורים שחורים מרובעים (`rounded-md bg-stone-900`) בסגנון האתר המקורי.
- **תמונות מקצועיות**: התמונות הקודמות (LoremFlickr) היו תמונות אקראיות מ-Flickr באיכות חובבנית שפגמו בתוצאה. הוחלפו בתמונות מ-[TheMealDB](https://www.themealdb.com/api.php) - API ציבורי חינמי (בלי מפתח) של מתכונים עם צילומי אוכל מקצועיים. הסקריפט `scripts/updateRecipeImages.js` מחזיק מיפוי משם המתכון בעברית למונח חיפוש באנגלית, שולף את `strMealThumb` ומעדכן את ה-DB. **שימו לב:** החיפוש ב-TheMealDB מתאים לשמות מנות ספציפיים - מונחים גנריים כמו "tuna salad" או "vegetable soup" מחזירים ריק, וצריך מונח שקיים באמת במאגר (למשל "salmon avocado salad"). כל 17 המתכונים קיבלו תמונה.
- **באג שנתקלנו בו:** הוספתי `overflow-x-hidden` על ה-div העוטף כדי לחסום גלילה אופקית מה-hero (שמשתמש ב-`w-screen`, כלומר 100vw שכולל את רוחב פס הגלילה). זה שבר את גלילת העמוד לגמרי - `div` עם `overflow-x` הופך לקונטיינר גלילה נפרד. הפתרון: `overflow-x: hidden` על `html` ב-`index.css` במקום על ה-div.

### קומפוננטת AiRecipeManager ✅
- `components/AiRecipeManager.jsx` - מסך אחד שמנהל את כל זרימת ה-AI, ב-route `/ai` (קישור נוסף ב-Navbar):
  - שדה קלט + כפתור "צור מתכון" (`POST /api/recipes/generate`), עם כפתורי דוגמה ללחיצה מהירה.
  - מצבי טעינה נפרדים לכל פעולה (`generating` / `modifying` / `saving`) - כך שכפתור אחד מראה ספינר בלי לנעול את השאר בטעות. בזמן יצירה מוצג גם skeleton מונפש.
  - תצוגת המתכון: כותרת, תיאור, זמן/מנות, מרכיבים כרשימה, ואופן ההכנה כרשימה **ממוספרת** עם עיגולים שחורים.
  - אזור "רוצים לשנות משהו?" ששולח ל-`POST /api/recipes/modify` ומחליף את המתכון המוצג בגרסה החדשה.
  - כפתור "שמור למתכונים שלי" (`POST /api/recipes/save`) שמנווט לעמוד המתכון שנוצר. אם המשתמש לא מחובר - במקום הכפתור מוצגת הודעה עם קישור להתחברות (במקום לתת ללחוץ ולקבל 401).
- `pages/RecipeDetail.jsx` עודכן להציג `description` ולפצל את `instructions` (מחרוזת עם `\n`) לרשימה ממוספרת - כך שגם מתכונים שנשמרו מה-AI וגם הישנים נראים נכון.
- **תמונה אוטומטית:** אחרי שהמתכון נוצר, הקומפוננטה קוראת ל-`/recipes/generate-image` **ברקע** (בלי `await` על ה-render) - כך שהמתכון מוצג מיד ואילו התמונה מגיעה אחריו, עם שלד טעינה בינתיים ("מכינים תמונה למנה..."). כישלון בהבאת תמונה לא מציג שגיאה אדומה למשתמש (רק `console.warn`) - מתכון בלי תמונה עדיין שימושי לגמרי.
  - ב-`handleModify`, אם הכותרת השתנתה - מביאים תמונה חדשה שתתאים למנה החדשה; אם לא - שומרים את הקיימת.
  - כפתור "תמונה אחרת" מאפשר להגריל תמונה מחדש.
- **נבדק מקצה לקצה בדפדפן:** הוקלד "מרק דלעת קרמי לחורף" → נוצר מתכון מלא (14 מרכיבים, 7 שלבים ממוספרים) → נשלחה בקשת שינוי "תעשה את זה חריף וללא קוקוס" → המתכון התעדכן נכון בתצוגה → נלחץ "שמור למתכונים שלי" → נשמר במסד וניווט אוטומטית לעמוד המתכון עם התיאור והשלבים הממוספרים.

## תכנון להמשך (טרם בוצע)
- אולי: הבחנה ויזואלית לתפקיד אדמין (badge, וכו').

