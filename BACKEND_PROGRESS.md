# Backend Progress - Recipe App

מעקב אחרי מה שנעשה בצד השרת (Node/Express/MongoDB).

## סטטוס נוכחי
🟢 פיצ'ר AI (יצירה/עריכת מתכון עם Gemini) עובד מקצה לקצה ונבדק בדפדפן.

## מה עשינו עד כה

### פיצ'ר AI - יצירה ועריכה של מתכונים ✅
- הותקן ה-SDK הרשמי `@google/genai`, ונוסף `GEMINI_API_KEY` ל-`.env` (**ריק - המשתמש צריך למלא**, מפתח חינמי מ-https://aistudio.google.com/apikey).
- `config/gemini.js` - עוטף את הלקוח:
  - `RECIPE_SCHEMA` - מבנה ה-JSON שה-AI חייב להחזיר (title, description, ingredients[], instructions[], prepTime, servings).
  - **נקודה חשובה:** Gemini תומך ב-`responseSchema` ברמת ה-API, כלומר המבנה נאכף על ידי הספק ולא רק "מבוקש" בטקסט הפרומפט. זה מייתר את הצורך לנקות ```json מסביב לתשובה ומונע שדות חסרים - הרבה יותר אמין מהגישה של "תחזיר לי JSON בבקשה".
  - הלקוח נוצר lazy (בקריאה הראשונה) ולא בטעינת הקובץ, כדי שהשרת יעלה תקין גם בלי מפתח ורק ה-routes של ה-AI ייכשלו עם הודעה ברורה (503).
- `routes/aiRecipes.js` - שלושה routes:
  - `POST /api/recipes/generate` - מקבל `prompt` חופשי, מחזיר מתכון JSON. פתוח (לא דורש התחברות).
  - `POST /api/recipes/modify` - מקבל `recipe` + `request` (למשל "תעשה את זה חריף יותר"), מחזיר את המתכון המעודכן. שולח ל-AI רק את שדות המתכון הרלוונטיים ולא את האובייקט הגולמי מהלקוח.
  - `POST /api/recipes/generate-image` - מייצר תמונה למתכון לפי שם ותיאור (ראו סעיף התמונות למטה).
  - `POST /api/recipes/save` - מוגן עם `protect`, שומר ל-DB. ה-`owner` נלקח מהטוקן (לא מה-body), ו-`aiGenerated` נקבע ל-true.
  - יש `asyncHandler` שמונע חזרה על try/catch, ולידציה על מבנה המתכון שמגיע מהלקוח, ו-error handler מרכזי בתחתית הקובץ.
- `models/Recipe.js` - נוספו `description` (String) ו-`aiGenerated` (Boolean).
  - **החלטת תכנון:** ה-AI מחזיר `instructions` כמערך שלבים, אבל בסכמה השדה נשאר `String` (עם `\n` בין השלבים) כדי לא לשבור את 17 המתכונים הקיימים ואת טופס העריכה הידני. ה-route מאחד את המערך לפני השמירה, והתצוגה מפצלת חזרה לרשימה ממוספרת.
- `server.js` - ה-routes של ה-AI נרשמים **לפני** `recipeRoutes` תחת אותו prefix, כדי ש-`/generate`, `/modify` ו-`/save` ייתפסו כמסלולים קבועים ולא יתנגשו עם `/:id`.
- **שתי תקלות שנתקלנו בהן בהרצה הראשונה (שווה לזכור):**
  1. **nodemon לא עוקב אחרי `.env`** - הוא מוגדר לקבצי `js,mjs,cjs,json` בלבד. אחרי שינוי ב-`.env` השרת ממשיך עם הערכים הישנים וצריך להפעיל אותו מחדש (או פשוט לשמור קובץ `.js` כלשהו כדי לאלץ restart). זה נראה כאילו המפתח לא נקלט.
  2. **שם המודל** - `gemini-2.5-flash` (שהיה ברירת המחדל שכתבנו) מחזיר 404 עם ההודעה "no longer available to new users" - גוגל חוסמת מודלים ישנים למפתחות חדשים. עברנו ל-`gemini-3.6-flash`. אם זה קורה שוב בעתיד, אפשר לראות את המודלים הזמינים למפתח עם `ai.models.list()`.
- **נבדק מקצה לקצה:** יצירה (`/generate`), עריכה (`/modify` - ביקשנו "חריף וללא קוקוס" והמתכון עודכן נכון כולל החלפת קרם קוקוס בשמנת ועדכון הכותרת והתיאור), שמירה (`/save`), ושמירה בלי טוקן נחסמת ב-401. המתכון נשמר במסד עם `aiGenerated: true` והבעלים הנכון.
### תמונה אוטומטית למתכון AI ⚠️ (עובד, אבל לא ביצירת AI אמיתית)
- `config/gemini.js` - נוספו `generateRecipeImage()` (יצירת תמונה במודל `gemini-3.1-flash-image`) ו-`suggestImageSearchTerm()` (מבקש מה-AI מונח חיפוש קצר באנגלית).
- `POST /api/recipes/generate-image` עובד בשני מסלולים:
  1. **מסלול ראשי** - יצירת תמונה אמיתית ב-AI, שמירה כקובץ ב-`backend/uploads/`, והחזרת URL. `server.js` מגיש את התיקייה סטטית תחת `/uploads`. (`uploads` נוסף ל-`.gitignore`).
  2. **מסלול גיבוי** - אם היצירה נכשלת, ה-AI מציע מונח חיפוש באנגלית ואנחנו שולפים תצלום אמיתי מ-TheMealDB.
  - התשובה כוללת `source: 'ai' | 'stock'` כדי שיהיה ברור מאיפה הגיעה התמונה.
- **⚠️ המצב בפועל כרגע: רץ תמיד במסלול הגיבוי.** יצירת תמונות **אינה כלולה ב-tier החינמי של Gemini** - כל מודלי התמונה (`gemini-3.1-flash-image`, `gemini-2.5-flash-image`, `gemini-3-pro-image`, `nano-banana-pro` ועוד) מחזירים 429 עם `quotaId` שמסתיים ב-`FreeTier`, בזמן שמודל הטקסט עובד מצוין באותו מפתח. **כדי להפעיל יצירת תמונות אמיתית צריך להפעיל חיוב (billing) בפרויקט ב-Google Cloud** - הקוד כבר מוכן ויעבור למסלול ה-AI אוטומטית ברגע שזה יקרה, בלי שינוי קוד.
- **חוב טכני ידוע:** תמונות שנוצרות ב-AI נשמרות לדיסק מיד, גם אם המשתמש לא ישמור את המתכון בסוף - כלומר עלולים להצטבר קבצים יתומים ב-`uploads/`. לפרויקט לימודי זה בסדר; בפרודקשן צריך ניקוי תקופתי או שמירה רק בזמן `/save`.

- `scripts/testAiFlow.js` - סקריפט בדיקה ידני לשלושת ה-endpoints. **שימו לב:** הוא פונה ל-`127.0.0.1` ולא ל-`localhost`, כי ה-`fetch` המובנה של Node פותר `localhost` ל-IPv6 (`::1`) בעוד השרת מאזין על IPv4 - זה גורם ל-"fetch failed" מבלבל.

### שדה imageUrl למתכון ✅
- נוסף שדה `imageUrl` (String, אופציונלי, ברירת מחדל `''`) ל-`models/Recipe.js`. אין ולידציה מיוחדת - זה סתם קישור טקסט, ה-frontend אחראי על תצוגה/נפילה חינה אם הקישור שבור.
- `routes/recipeRoutes.js` לא היה צריך שינוי - השדה עובר אוטומטית דרך `req.body` גם ב-POST וגם ב-PUT.

### סקריפט Seed למתכונים ✅
- `scripts/seedRecipes.js` - סקריפט חד-פעמי (`node scripts/seedRecipes.js`) שמוסיף כ-17 מתכונים אמיתיים (חומוס, שקשוקה, שניצל, פלאפל, סביח, עוגיות שוקולד וכו') ומשייך אותם למשתמש הראשון שקיים ב-DB. אם מתכון עם אותו שם כבר קיים - מריץ בלי ליצור כפילות, ורק מעדכן את התמונה שלו (`imageUrl`) - כך שאפשר להריץ שוב בבטחה כדי לרענן תמונות.
- לכל מתכון בסקריפט יש `imageUrl` שנבנה עם helper בשם `foodImage(keyword)` - פונה לשירות הציבורי [LoremFlickr](https://loremflickr.com/) שמחזיר תמונה אמיתית מ-Flickr לפי תגית טקסט (למשל `hummus`), בלי צורך במפתח API. **הערה חשובה:** בלי הסיומת `/all` בסוף ה-URL, LoremFlickr מתאים לפי *כל* תגית בנפרד (OR) ולא לפי כולן יחד - זה גרם להתאמות מוזרות (למשל תמונת כוס KFC למתכון פנקייקים). עם `/all` ההתאמה משמעותית יותר טובה ומדויקת. עדיין ייתכנו התאמות לא מושלמות כי זו תמונה אקראית מתוך Flickr - זה בסדר לפרויקט לימודי/הדגמה, אבל לא מתאים לפרודקשן אמיתי (כדאי שם להעלות תמונות אמיתיות/מצולמות, לא להסתמך על שירות אקראי חיצוני).

### Populate לשם היוצר ✅
- `GET /api/recipes` ו-`GET /api/recipes/:id` עכשיו עושים `.populate('owner', 'name')` - במקום רק ה-id של היוצר, מחזירים אובייקט `{ _id, name }`. (POST/PUT עדיין מחזירים owner כ-id רגיל, זה בסדר כי הפרונטאנד עושה GET טרי אחרי יצירה/עריכה).
- **שימו לב אם עובדים על זה שוב:** זה שינוי breaking בצורת ה-JSON של `owner` ב-response - קוד פרונטאנד שמשווה `owner` ישירות ל-id צריך להשוות ל-`owner._id`.

### חיפוש/סינון מתכונים ✅
- `GET /api/recipes?search=...` - אם יש פרמטר `search`, מסננים לפי regex (case-insensitive) על `title` או על איברי המערך `ingredients` (`$or`). בלי הפרמטר מחזיר הכל כרגיל.
- תווים מיוחדים ב-`search` נשארים escaped לפני בניית ה-regex, כדי שקלט מהמשתמש (כמו `.` או `*`) לא יתפרש כתחביר regex.
- נבדק עם curl: חיפוש שמתאים לשם, חיפוש שמתאים למרכיב בלבד, חיפוש בלי תוצאות, ובלי פרמטר בכלל - כולם עבדו נכון.

### שלב 6+7: חיבור Recipe ל-User + הגנה על routes ✅
- נוסף שדה `owner` ל-`models/Recipe.js` - `ObjectId` עם `ref: 'User'` (חובה).
- `routes/recipeRoutes.js` עודכן:
  - `GET /` ו-`GET /:id` נשארו פתוחים לכולם (אין צורך בהתחברות כדי לצפות במתכונים).
  - `POST /` מוגן עם `protect` - ה-`owner` נקבע מ-`req.user._id` (מהטוקן), לא מגוף הבקשה, כדי שאי אפשר "ליצור מתכון בשם מישהו אחר".
  - `PUT /:id` ו-`DELETE /:id` מוגנים עם `protect`, ובנוסף בודקים שהמשתמש המחובר הוא הבעלים (`recipe.owner`) או `role === 'admin'` - אחרת 403. גם כאן, שינוי `owner` דרך ה-body נחסם (`delete req.body.owner`).
  - PUT עבר משימוש ב-`findByIdAndUpdate` לטעינת המסמך קודם (`findById`) + `Object.assign` + `save()`, כי צריך את המסמך הטעון מראש כדי לבדוק בעלות לפני העדכון.
- נבדק ידנית תרחיש מלא: POST בלי טוקן (401) → הרשמת 2 משתמשים → יצירת מתכון ע"י משתמש A → משתמש B מנסה לערוך/למחוק (403 בשני המקרים) → משתמש A מצליח לערוך את שלו → ניסיון לשנות `owner` דרך ה-body נחסם בהצלחה → קידום משתמש B ל-`admin` ידנית ב-DB → משתמש B (אדמין) מצליח לערוך ולמחוק את המתכון של A. הכל עבד כצפוי. נוקו משתמשי הבדיקה.

### שלב 5: Middleware להרשאות (משתמש רגיל מול אדמין) ✅
- נוצר `middleware/auth.js` עם שני middlewares:
  - `protect` - קורא header בשם `Authorization: Bearer <token>`, מאמת עם `jwt.verify` מול `JWT_SECRET`, שולף את המשתמש מה-DB (`User.findById`) ומצמיד אותו ל-`req.user`. אם אין טוקן / הטוקן לא תקין / פג תוקף / המשתמש נמחק - מחזיר 401.
  - `admin` - רץ אחרי `protect`, בודק `req.user.role === 'admin'`, אחרת מחזיר 403. (עדיין לא בשימוש בפועל - יחובר ל-routes ספציפיים בשלב 6).
- נוסף route לבדיקה: `GET /api/auth/me` (מוגן עם `protect`) - מחזיר את פרטי המשתמש המחובר. נבדק: בלי טוקן (401), עם טוקן תקין (200 + פרטי משתמש), עם טוקן מזויף (401).
- `admin` נבדק בנפרד (סימולציה עם req/res מדומים) - משתמש רגיל נחסם (403), אדמין עובר הלאה.

### שלב 4: מודל User + הרשמה/התחברות עם JWT ✅
- הותקנו `bcryptjs` (הצפנת סיסמאות) ו-`jsonwebtoken` (הנפקת טוקנים).
- נוסף `JWT_SECRET` (מחרוזת אקראית ארוכה) ל-`.env`.
- נוצר `models/User.js` - שדות: `name`, `email` (ייחודי, ולידציה של פורמט), `password` (מינימום 6 תווים, `select: false` כדי שלא יחזור בשאילתות רגילות), `role` (`user`/`admin`, ברירת מחדל `user`).
  - `pre('save')` hook מצפין את הסיסמה אוטומטית לפני שמירה (רק אם היא השתנתה).
  - **תקלה שנתקלנו בה ופתרונה:** הגרסה המותקנת היא Mongoose 9 (גרסה חדשה מאוד), ובה בוטל סגנון ה-callback הישן `function(next) {...}` ב-hooks - הכל async/await בלבד עכשיו, בלי קריאה ל-`next()`. קיבלנו שגיאת `next is not a function` עד שהבנו את זה. אם יוצרים hooks נוספים בעתיד - לכתוב `async function() {...}` בלי פרמטר `next`.
  - מתודת `comparePassword` להשוואת סיסמה גלויה מול ההאש השמור.
- נוצר `utils/generateToken.js` - מנפיק JWT עם תוקף 30 יום, מכיל את מזהה המשתמש.
- נוצר `routes/authRoutes.js` עם 2 routes תחת `/api/auth`:
  - `POST /register` - בודק אימייל כפול, יוצר משתמש, מחזיר פרטי משתמש + טוקן.
  - `POST /login` - מאתר לפי אימייל (כולל שליפת password עם `+password`), משווה סיסמה, מחזיר פרטי משתמש + טוקן. הודעת שגיאה כללית ("אימייל או סיסמה שגויים") גם כשהאימייל לא קיים וגם כשהסיסמה שגויה, מטעמי אבטחה (לא לחשוף אילו אימיילים רשומים).
- חובר ל-`server.js` עם `app.use('/api/auth', authRoutes)`.
- נבדק ידנית: הרשמה, התחברות עם סיסמה נכונה/שגויה, הרשמה עם אימייל כפול, ולידציה על סיסמה קצרה מדי, התחברות עם אימייל לא קיים - הכל עבד כצפוי.

### שלב 3: מודל Recipe + routes בסיסיים (CRUD) ✅
- נוצר `models/Recipe.js` - Mongoose schema עם שדות: `title` (חובה), `ingredients` (מערך מחרוזות, חובה, לפחות אחד), `instructions` (חובה), `prepTime` (דקות, ברירת מחדל 0), `servings` (ברירת מחדל 1). עם `timestamps: true` (מוסיף `createdAt`/`updatedAt` אוטומטית).
- נוצר `routes/recipeRoutes.js` עם 5 routes תחת `/api/recipes`:
  - `GET /` - כל המתכונים (ממוינים לפי `createdAt` יורד).
  - `GET /:id` - מתכון בודד.
  - `POST /` - יצירת מתכון.
  - `PUT /:id` - עדכון מתכון (`runValidators: true` כדי לאכוף את חוקי ה-schema גם בעדכון).
  - `DELETE /:id` - מחיקת מתכון.
  - כולל טיפול בשגיאות: `ValidationError` (שדה חובה חסר) → 400, `CastError` (id לא תקין) → 400, מתכון לא נמצא → 404.
- חובר ל-`server.js` עם `app.use('/api/recipes', recipeRoutes)`.
- נבדק ידנית עם curl: יצירה (כולל טקסט בעברית - תקין), קריאת הכל, קריאת בודד, id לא תקין, עדכון, ולידציה על שדות חסרים, מחיקה, וידוא 404 אחרי מחיקה - הכל עבד כצפוי.

### שלב 2: חיבור ל-MongoDB Atlas עם Mongoose ✅
- נוצר cluster חינמי (M0) ב-MongoDB Atlas, משתמש מסד בשם `admin`.
- הותקן `mongoose`.
- נוצר `config/db.js` שמתחבר לפי `MONGO_URI` מ-`.env` (שם המסד: `recipeApp`).
- **תקלה שנתקלנו בה ופתרונה:** על המחשב הזה (Windows), חיבור עם `mongodb+srv://` נכשל בשגיאת `querySrv ECONNREFUSED` - זו בעיה ידועה שבה ה-DNS resolver המובנה של Node לא מצליח לבצע שאילתת SRV, גם כשה-DNS הרגיל של המערכת תקין. הפתרון: `dns.setServers(['8.8.8.8'])` בתחילת `config/db.js`, לפני `mongoose.connect`. אם בעתיד עוברים למחשב/רשת אחרים ותופיע שוב השגיאה הזו - זה כנראה אותה סיבה.
- הערה נוספת (לא קשור לבאג, רק FYI): גרסת `dotenv` המותקנת (17.4.2) מדפיסה "טיפ" פרסומי בקונסול בכל טעינה (תכונה מכוונת של הספרייה, לא נוזק). השתקנו את זה עם `dotenv.config({ quiet: true })`.
- אימות: הרצנו את השרת וראינו בלוג גם `Server running on port 5000` וגם `MongoDB Connected: ...mongodb.net`.

### שלב 1: שרת Express בסיסי ✅
- הוקמה תיקיית `backend/` עם `npm init`.
- הותקנו חבילות: `express`, `dotenv`, `cors` (רגילות), `nodemon` (dev).
- נוצר `server.js`:
  - `express()` - יוצר את אפליקציית השרת.
  - `app.use(cors())` - מאפשר לפרונטאנד לדבר עם השרת (מדפדפן/פורט אחר).
  - `app.use(express.json())` - מאפשר לקרוא JSON מגוף בקשות (`req.body`).
  - route `GET /` שמחזיר הודעת בדיקה.
  - השרת מאזין על פורט מ-`.env` (ברירת מחדל 5000).
- נוצר `.env` עם `PORT=5000`.
- נוצר `.gitignore` (מתעלם מ-`node_modules` ו-`.env`).
- הרצנו את השרת ובדקנו עם `curl http://localhost:5000/` - קיבלנו תשובה תקינה.

## איך מריצים
```
cd backend
npm run dev   # מריץ עם nodemon (הפעלה מחדש אוטומטית בשינוי קוד)
```

## השלב הבא (טרם בוצע)
הבקאנד הבסיסי (Recipe CRUD + Auth + הרשאות) שלם. אפשרויות להמשך:
- דרך אחת: להתחיל את הפרונטאנד (React) - הגיע הזמן, ראו `FRONTEND_PROGRESS.md`.
- דרך אחרת: להוסיף פיצ'רים לבקאנד (חיפוש/סינון מתכונים, pagination, תמונות, endpoint לרשימת "המתכונים שלי").
